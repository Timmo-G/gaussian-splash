# 第7-8周 练习题（分级题库）

---

## 基础题（必做）

### B1. 相机投影手推

给定相机参数：
- 内参：fx=fy=500，cx=320，cy=240
- 外参：相机绕Y轴旋转45°，位于世界坐标 (3, 0, 5) 处

1. 手推旋转矩阵 R 和平移向量 t（注意：t是世界原点在相机坐标系中的位置）
2. 将世界坐标点 P=(0, 0, 0) 投影到图像坐标
3. 验证：相机在世界坐标系中的位置 C = -Rᵀt
4. 如果相机距离物体加倍（t变为2t），投影点的位置如何变化？

```python
import numpy as np
theta = np.radians(45)
R = np.array([[np.cos(theta), 0, np.sin(theta)],
              [0, 1, 0],
              [-np.sin(theta), 0, np.cos(theta)]])
# 注意：t不是相机位置，而是世界原点在相机坐标系中的位置
cam_pos_world = np.array([3, 0, 5])
t = -R @ cam_pos_world  # 正确的t计算方式
```

---

### B2. Alpha合成实现

实现前向Alpha合成：

```python
def alpha_composite(colors, alphas):
    """
    C = Σᵢ cᵢ · αᵢ · Πⱼ<ᵢ (1-αⱼ)
    colors: N×3
    alphas: N
    返回: (3,) 合成颜色
    """
    pass
```

测试：
1. 3个完全不透明的层（α=1），只能看到最前面的
2. 3个半透明的层（α=0.5），颜色如何混合？
3. 验证：当所有α→0时，结果趋向于什么？

---

### B3. 体积渲染

实现离散体积渲染：

```python
def volume_render(sigmas, colors, deltas):
    """
    C = Σᵢ Tᵢ · (1-exp(-σᵢδᵢ)) · cᵢ
    Tᵢ = exp(-Σⱼ<ᵢ σⱼδⱼ)
    """
    pass
```

测试：
1. 单个物体（密度集中在某一深度）
2. 两个物体（前景遮挡后景）
3. 对比：体积渲染 vs Alpha合成，在什么情况下等价？

---

### B4. 高斯投影

实现3D高斯到2D的雅可比近似投影：

```python
def project_gaussian(mu_3d, Sigma_3d, K, R_cam, t_cam):
    """
    返回：mu_2d (2,), Sigma_2d (2×2), depth (float)
    """
    pass
```

验证：
1. 当高斯在相机正前方（z轴上），协方差为单位矩阵时，投影结果是什么？
2. 当深度z增大时，投影高斯的大小如何变化？（应该变小）

---

### B5. 焦距与视角

1. 计算以下相机的水平视角（FoV）：
   - fx=500，图像宽度W=640
   - fx=1000，图像宽度W=640
   - fx=200，图像宽度W=640

2. 公式：FoV_x = 2 · arctan(W / (2·fx))

3. 解释：为什么3DGS训练时需要知道相机的FoV？

---

## 进阶题（推荐）

### P1. 从OpenGL到3DGS

你已经写过OpenGL渲染器，对比以下两种渲染管线：

| 步骤 | OpenGL | 3DGS |
|------|--------|------|
| 几何表示 | 三角形网格 | 高斯椭球体 |
| 顶点处理 | 顶点着色器 | ? |
| 光栅化 | 三角形光栅化 | ? |
| 片段处理 | 片段着色器 | ? |
| 深度测试 | Z-buffer | ? |

填写3DGS对应的步骤，并解释每个步骤的具体操作。

---

### P2. Tile-based渲染实现

实现一个简化的Tile-based渲染器：

```python
def tile_based_render(gaussians_2d, W, H, tile_size=16):
    """
    gaussians_2d: list of (mu_2d, Sigma_2d, color, opacity, depth)
    
    步骤：
    1. 对每个高斯，找到它覆盖的tile范围
    2. 创建 (tile_id, depth) 排序键
    3. 按排序键排序
    4. 每个tile独立执行alpha合成
    """
    pass
```

对比：逐像素渲染 vs Tile-based渲染，在什么情况下Tile-based更高效？

---

### P3. 完整渲染管线

修改 `demo_05_完整渲染管线.py` 中的 `render_full` 函数，添加以下功能：

1. **背景颜色**：当透射率 T > 0.1 时，添加天空蓝色背景
2. **法线图**：输出每个像素的法线方向（用高斯的主轴方向近似）
3. **渲染统计**：记录每个高斯被多少像素使用，找出"无效高斯"（贡献极小的高斯）

---

### P4. 相机标定

给定一组3D-2D对应点，用DLT（直接线性变换）估计相机内参：

```python
def estimate_intrinsics(points_3d, points_2d):
    """
    points_3d: N×3 世界坐标
    points_2d: N×2 图像坐标
    假设已知外参（R=I, t=0），估计内参K
    """
    # 构建线性方程组 Ax = 0
    # 用SVD求解
    pass
```

测试：用已知内参生成投影点，然后用DLT恢复内参，验证精度。

---

### P5. NeRF vs 3DGS渲染对比

实现简化的NeRF体积渲染，与3DGS渲染对比：

```python
def nerf_render_pixel(ray_origin, ray_dir, density_fn, color_fn,
                      t_near=0.1, t_far=10.0, n_samples=64):
    """
    沿光线采样，执行体积渲染
    density_fn: 给定3D点，返回密度
    color_fn: 给定3D点和方向，返回颜色
    """
    pass
```

对比：
1. 渲染同一场景，NeRF和3DGS的结果有什么差异？
2. 渲染速度对比（每像素需要多少次函数调用？）
3. 为什么3DGS比NeRF快？

---

## 挑战题（选做）

### C1. 与官方代码对接

阅读3DGS官方代码：

1. `gaussian_renderer/__init__.py`：理解 `render()` 函数的输入输出
2. `submodules/diff-gaussian-rasterization/rasterize_points.cu`：理解CUDA光栅化器的核心逻辑
3. 回答：
   - 官方代码中的 `preprocess` 步骤做了什么？
   - `forward` 函数中的 `tile_bounds` 是如何计算的？
   - 为什么需要 `backward` 函数？（可微渲染）

---

### C2. 实现可微渲染

用PyTorch实现一个简单的可微2D高斯渲染器，并用梯度下降优化高斯参数：

```python
import torch

def render_gaussian_2d_torch(mu, Sigma, color, alpha, image_size=(64, 64)):
    """
    用PyTorch实现，支持自动微分
    """
    pass

def optimize_to_target(target_image, n_gaussians=5, n_steps=500):
    """
    给定目标图像，用梯度下降找到最优的高斯参数
    """
    # 初始化参数（随机）
    # 渲染
    # 计算损失
    # 反向传播
    # 更新参数
    pass
```

---

### C3. COLMAP数据处理

如果你有一组建筑照片，用COLMAP处理并可视化结果：

```bash
# COLMAP命令行
colmap feature_extractor --database_path db.db --image_path images/
colmap exhaustive_matcher --database_path db.db
colmap mapper --database_path db.db --image_path images/ --output_path sparse/
```

用Python可视化COLMAP输出：
```python
# 读取COLMAP输出的相机参数和稀疏点云
# 可视化相机位置和点云
# 这就是3DGS训练的输入！
```

---

## 思考题

1. 为什么3DGS比NeRF快？（从渲染管线的角度，列出至少3个原因）
2. 雅可比近似在什么情况下误差较大？（提示：高斯离相机很近，或高斯很大时）
3. 3DGS的深度排序是全局的还是每像素的？这有什么影响？（提示：透明物体的渲染顺序）
4. 如果一个高斯基元的2D投影协方差矩阵接近奇异（行列式接近0），会发生什么？官方代码如何处理？
5. Tile-based渲染为什么适合GPU并行化？（提示：每个tile独立，无数据依赖）
6. 3DGS的渲染是否支持实时（>30fps）？需要什么硬件？
